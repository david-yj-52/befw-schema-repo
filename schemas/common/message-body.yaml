MessageBodyBase:
  type: object
  required: [siteId, userId]
  properties:
    siteId: {type: string}
    userId: {type: string}


MessageBodyManufacture:
  allOf:
    - $ref: "#/MessageBodyBase"
  type: object
#  required: [siteId, userId]
  properties:
    lotId: {type: string}
    eqpId: {type: string}
    carrId: {type: string}
    portId: {type: string}

MessageBodyCommerce:
  allOf:
    - $ref: "#/MessageBodyBase"
  type: object
#  required: [siteId, userId]
  properties:
    prodId: {type: string}
    prodNm: {type: string}