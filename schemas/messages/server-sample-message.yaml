title: ServerSampleMessage
type: object
required: [head, body]
properties:
  head:
    allOf:
      - $ref: "../common/message-head.yaml"
      - type: object
        required: [event]
        properties:
          event:
            type: string
            default: SERVER_SAMPLE_MESSAGE
  body:
    allOf:
      - $ref: "../common/message-body.yaml#/MessageBodyBase"
      - type: object
        required: [useYn]
        properties:
          useYn: {type: string}